const initialState: any = {}

export const appReducer = (state = initialState, action: ActionsType): any => {
    switch (action.type) {
        default:
            return state
    }
}

// actions

// thunks

// types

type ActionsType = any
